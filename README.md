# JMJ AEC Services LLP — Static Website

This is a React + Material UI static website scaffold for JMJ AEC Services LLP.

Quick start:

1. Install dependencies

```bash
npm install
```

2. Run in development

```bash
npm start
```

Build for production:

```bash
npm run build
```

Notes:
- Uses React, React Router, and MUI v5.
- Contact form shows a demo success message (no backend).
